<%= content_for :dashboard_content do %>
  <div class="card">
    <div class="card-header card-header-primary"><h3>Add achievement</h3></div>
    <div class="card-body">
      <%= render 'form', admin_achievement: @admin_achievement %>
      <%= link_to 'Back', admin_achievements_path, class: "btn btn-default" %>
    </div>
  </div>
<% end %>
<%= render template: 'layouts/material_dashboard' %>