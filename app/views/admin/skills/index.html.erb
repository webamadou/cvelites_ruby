<%= content_for :dashboard_content do %>
  <div class="card">
    <div class="card-heading card-heading-primary">
      <p id="notice"><%= notice %></p>
      <h3>Admin Skills</h3>
    </div>
    <div class="card-body">
      <table id="skills-dt" class="table table-bordered table-striped" width="100%" data-source="<%= admin_skills_path %>">
        <%= link_to 'New Admin Skill', new_admin_skill_path, class: 'btn btn-primary' %>
        <thead>
          <tr>
            <th>Name</th>
            <th>Description</th>
            <th></th>
          </tr>
        </thead>
        <tbody></tbody>
        <!--<tbody>
          <%# @admin_skills.each do |admin_skill| %>
            <tr>
              <td><%#= admin_skill.name %></td>
              <td><%#= admin_skill.description %></td>
              <td><%#= link_to 'Show', admin_skill %></td>
              <td><%#= link_to 'Edit', edit_admin_skill_path(admin_skill) %></td>
              <td><%#= link_to 'Destroy', admin_skill, method: :delete, data: { confirm: 'Are you sure?' } %></td>
            </tr>
          <%# end %>
        </tbody>-->
      </table>
    </div>
  </div>
<% end %>
<%= render template: 'layouts/material_dashboard' %>